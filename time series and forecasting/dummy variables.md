if there is an event (say, a shock) between some values that cannot be estimated correctly, we can use dummy variables to estimate.
- 1 during the dummy event
- 0 if not

linear trend model with a dummy variable can look something like:
$$y_{t} = \alpha+\beta_{1}\text{ Time} + \beta_{2}\text{ Shutdown}+\epsilon_{t}$$

